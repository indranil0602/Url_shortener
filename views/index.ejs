<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <title>Tiny Url</title>
</head>
<body>
    <div class="container">
        <br><br>
        <h1>Big think, click shrink, sort link</h1>
        <form action="/shortUrls" method="POST" class="row g-3 my-4 form-inline">
            <input required placeholder="url" type="url" name="longUrl" id="longUrl" class="form-control col-auto mr-2">
            <button class="btn btn-success col-auto" type="submit">Shrink</button>
        </form>

        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Long Url</th>
                    <th>Tiny Url</th>
                    <!-- <th>Clicks</th> -->
                </tr>
            </thead>
            <tbody>
                <% shortUrls.forEach(shortUrl => { %>
                    <tr>
                        <td style="word-wrap: break-word;min-width: 600px;max-width: 160px;"><a href="<%= shortUrl.full %>"><%= shortUrl.full %> </a></td>
                        <td><a href="<%= shortUrl.short %>"><%= shortUrl.short %></a></td>
                        <!-- <td><%= shortUrl.clicks %></td> -->
                    </tr>
                <% })  %> 
            </tbody>
        </table>
    </div>
</body>
</html>